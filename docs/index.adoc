= 产品设计文档:订单履约系统
:keywords: 设计文档, 订单履约, docs-as-code

[NOTE]
====
本页及其子页由 Git 仓库经 *Confluence Publisher CLI* 自动发布(Git 为唯一事实来源)。
**请勿在 Confluence 直接编辑** —— 改动会在下次同步时被覆盖。
====

本文档演示「AsciiDoc(含 PlantUML)→ Confluence」的 docs-as-code 发布流水线。

按 Confluence Publisher 的约定,本目录结构映射为 Confluence 页面树:`index.adoc` 是根页,
`index/` 文件夹下的每个 `.adoc` 成为它的子页。

子页:

* 概述
* 系统架构
* 履约流程
