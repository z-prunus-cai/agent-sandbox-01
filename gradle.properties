# Performance: parallel module execution + build caching.
org.gradle.parallel=true
org.gradle.caching=true

# Set -Pskip.frontend=true to skip the (slow) frontend npm build when you are
# only iterating on backend/batch code. Backend will then serve no SPA, which
# is fine for API-only development.
skip.frontend=false
