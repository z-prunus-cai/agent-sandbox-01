# Round3c · L3-04 面向对象与程序设计范式 · 报告 prompt（P1）

```text
[P1] L3-04·大主题1 编程范式总览与抽象层次 — 报告prompt

【任务】为「L3-04·大主题1 编程范式总览与抽象层次」产出一份 report-format v3（广度优先的教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 5 个小主题（1.1–1.5），属"范式总览"型、跨机制少。先判定"出 1 份 or 拆 N 份 + 理由"（依据：小主题数、是否跨机制、预计篇幅）。倾向 1 份；若判拆分，用 -a/-b 后缀。把判定与理由写在报告抬头下。

【v3 格式硬性（必须遵守）】
- 标题层级固定：`#`（报告标题=本大主题）→ `##`（小主题=章节）→ `###`（每个内容项一节）。
- `###` 节下不写「核心概念：」「辅助说明：」等任何标签，直接正文、靠空行分段。每节含：核心说明（是什么/定义/结论，正确）+ 充分的辅助说明（面向初学者：直觉/为什么/最小例子/易错点/关联，写到初学者能懂为止，可多段——这是必需，不是点缀）。
- 任何公式独占一行（块级），不内联埋进句子。
- 代码片段独占代码块，不与正文混排。
- 教辅口吻；广度优先：讲到初学者懂即可，不做专家级纵深（不写长篇机制深挖/完整推导/设计权衡长论）。
- 抬头带可 grep 基线串：`基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`；标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可直接读 `study/cs-kb/round3b-subtopics/g02-programming-ds.md` 中 L3-04 的「大主题 1」小节取完整条目；本 prompt 已自足复述关键项：
- 1.1 命令式/过程式范式 — 语句序列 + 状态变更（L1-01 默认写法在此升华）。
- 1.2 面向对象范式概览 — 对象 + 消息（SICP Ch3 引）。
- 1.3 函数式范式概览 — 表达式 + 无副作用（SICP §1.3）。
- 1.4 声明式/逻辑式范式概览 — 描述「是什么」（SICP Ch4 引；深挖归 L5-06，此处仅广度）。
- 1.5 抽象作为复杂度管理 — 范式=风格而非语言、多范式融合。
广度完备：小主题下内容项要查全，宁可多列浅讲，不遗漏。

【多来源比对（强制，不可省）】
- 每个内容项须用 ≥2 个独立来源交叉核对，优先一手（SICP 教材章节 / CMU15-150 syllabus / GoF / 官方语言规范）。
- 来源冲突时两边都记、点明分歧、不和稀泥。
- 不编造：具体表述/分类/版本来自比对通过的来源，或标「待核」，绝不凭记忆填。
- Python/JS 示例可给（用同一问题的三范式对照最直观）；示例代码独占块。
- 每个 `##` 章节末集中列「来源与时效」：锚点（教材§/规范/文档+版本+核实日期）、前沿项标演进快、未证实项标待核、冲突项两边定位。不逐项脚注。

【权威锚点清单（取自 round3a）】
- SICP《Structure and Interpretation of Computer Programs》(Abelson & Sussman)，https://sarabander.github.io/sicp/html/index.xhtml ——本大主题主锚 Ch1（Building Abstractions with Procedures）+ 引 Ch2/Ch3/Ch4 作范式概览。
- round2 表 C（范式分类背景）。
- 补充可参 CMU 15-150 syllabus 作 FP 概览旁证。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；本大主题实际落盘 `study/cs-kb/findings/011-oop-paradigms-t1-paradigm-overview.md`（若拆分用 `...-t1-paradigm-overview-a/-b.md`）。subagent 自己 Write，报告须自足（复现代码/命令/环境串贴进正文）。不得残留工具标签或控制字符。
```

```text
[P1] L3-04·大主题2 数据抽象与封装 — 报告prompt

【任务】为「L3-04·大主题2 数据抽象与封装」产出一份 report-format v3（广度优先的教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 4 个小主题（2.1–2.4），偏概念、跨机制少。先判定"出 1 份 or 拆 N 份 + 理由"。倾向 1 份；若判拆分，用 -a/-b 后缀。判定与理由写在报告抬头下。

【v3 格式硬性（必须遵守）】
- 层级固定：`#`（报告标题=本大主题）→ `##`（小主题）→ `###`（每内容项一节）。
- `###` 节下不写标签，直接正文分段：核心说明（正确）+ 充分辅助说明（面向初学者：直觉/为什么/最小例子/易错点/关联，讲到懂为止，可多段，必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲到初学者懂但不做专家纵深。
- 抬头带基线串 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `study/cs-kb/round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 2」小节取全；关键项复述：
- 2.1 构造器/选择器与抽象屏障 — 分层隔离表示（SICP §2.1；如有理数抽象）。
- 2.2 数据的过程式表示 — 闭包性质、以函数表数据（SICP §2.1.3；如闭包造 pair）。
- 2.3 信息隐藏与 ADT 规范 — 隐藏内部表示（Liskov 1987）。
- 2.4 接口/实现分离与表示不变式 — 抽象函数与表示不变式（Liskov 1987）。
广度完备：内容项查全，多列浅讲不遗漏。

【多来源比对（强制）】
- 每项 ≥2 独立源交叉核对，优先一手（SICP 章节 / Liskov 1987 论文 / 官方规范）；冲突两边都记、点明分歧。
- 不编造；未证实项标「待核」，标核实日期。
- Python/JS 示例可给，独占代码块。
- 每个 `##` 章节末集中列「来源与时效」（锚点+版本+核实日期、待核项、冲突项定位），不逐项脚注。

【权威锚点清单（取自 round3a）】
- SICP §2.1-2.2（Building Abstractions with Data：数据抽象/闭包性质），https://sarabander.github.io/sicp/html/index.xhtml 。
- Barbara Liskov《Data Abstraction and Hierarchy》(1987)——信息隐藏/ADT/抽象函数与不变式。
- 关联：↔L2-01（ADT 的结构实现，勿深挖结构本身）。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；本大主题实际落盘 `study/cs-kb/findings/011-oop-paradigms-t2-data-abstraction.md`（拆分用 `...-a/-b`）。subagent 自己 Write，报告自足，不残留工具标签/控制字符。
```

```text
[P1] L3-04·大主题3 OO 三支柱：封装/继承/多态 — 报告prompt

【任务】为「L3-04·大主题3 OO 三支柱：封装/继承/多态」产出一份 report-format v3（广度优先教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 5 个小主题（3.1–3.5），是 OO 核心、内容项较密。先判定"1 份 or 拆 N 份 + 理由"。倾向 1 份；若小主题讲解体量偏大可考虑拆分，用 -a/-b。判定与理由写在抬头下。

【v3 格式硬性（必须遵守）】
- 层级 `#`→`##`→`###`；`###` 下不写标签，直接正文分段：核心说明（正确）+ 充分辅助说明（初学者直觉/为什么/最小例子/易错点/关联，讲到懂、可多段、必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲懂不纵深。
- 抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 3」；关键项复述：
- 3.1 对象=状态+行为、局部状态与封装 — 消息传递中的可变状态（SICP §3.1）。
- 3.2 类、实例与消息传递模型 — 属性/方法查找（Python Data Model；如 `__dict__`）。
- 3.3 继承与 is-a 关系 — 子类扩展/覆盖（单继承覆盖）。
- 3.4 子类型多态与方法覆盖 — 运行时按对象选行为（SICP §2.4-2.5）。
- 3.5 多重表示与通用型操作 — 数据导向编程、按类型分派（SICP §2.4-2.5；如 `functools.singledispatch`）。
广度完备：内容项查全。

【多来源比对（强制）】
- 每项 ≥2 独立源，优先一手（SICP 章节 / Python 3.11 Data Model 官方文档 / 经典权威教材）；冲突两边都记、点明分歧。
- 不编造具体协议名/查找规则；未证实项标「待核」，标核实日期。
- Python/JS 示例可给，独占块。
- 每个 `##` 章节末集中列「来源与时效」，不逐项脚注。

【权威锚点清单（取自 round3a）】
- SICP §3.1（局部状态与对象）、§2.4-2.5（多重表示/通用型操作/数据导向编程），https://sarabander.github.io/sicp/html/index.xhtml 。
- Python 3.11 Data Model（`__`协议、属性/方法查找、`__dict__`），官方文档。
- 关联：派发的机器层落地归 L3-04·大主题4 与 L3-03，此处只讲语义/用法。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；实际落盘 `study/cs-kb/findings/011-oop-paradigms-t3-oo-pillars.md`（拆分 `...-a/-b`）。subagent 自 Write，报告自足，不残留工具标签/控制字符。
```

```text
[P1] L3-04·大主题4 动态派发机制 — 报告prompt

【任务】为「L3-04·大主题4 动态派发机制」产出一份 report-format v3（广度优先教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 5 个小主题（4.1–4.5），跨"语义对比 + C++ vtable + Python MRO/多继承"多机制。先判定"1 份 or 拆 N 份 + 理由"。若 vtable 与 MRO 两线体量都足，可考虑拆 2 份（-a 静/动派发与 vtable、-b 鸭子类型与 MRO/菱形）；否则 1 份。判定与理由写在抬头下。

【v3 格式硬性（必须遵守）】
- 层级 `#`→`##`→`###`；`###` 下不写标签，正文分段：核心说明（正确）+ 充分辅助说明（初学者直觉/为什么/最小例子/易错点/关联，讲到懂、必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲懂不纵深。
- 抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 4」；关键项复述：
- 4.1 静态派发 vs 动态派发 — 编译期绑定 vs 运行期绑定对比。
- 4.2 虚表（C++ vtable）机制 — 虚函数指针表概念（一手规范章节待核；机器层落地深挖归 L3-03，此处只讲概念/用法）。
- 4.3 鸭子类型与运行时属性查找 — 无需继承的结构化多态（Python）。
- 4.4 方法解析顺序 MRO 与 C3 线性化 — `__mro__` 顺序规则（Python Data Model）。
- 4.5 菱形继承问题 — 多继承二义性与 C3/super() 解决。
广度完备：内容项查全。

【多来源比对（强制）】
- 每项 ≥2 独立源，优先一手（Python 3.11 Data Model 官方文档 / C++ 标准或权威教材 / C3 线性化原始描述）；冲突两边都记。
- 4.2 vtable 一手规范章节属「待核」，未坐实不下结论、如实标注；不编造下标/布局。
- Python 示例可给（打印 `Cls.__mro__`、diamond + super 链），独占块；C++ vtable 观察可作旁证。
- 每个 `##` 章节末集中列「来源与时效」（含待核项与冲突项定位），不逐项脚注。

【权威锚点清单（取自 round3a）】
- Python 3.11 Data Model（`__mro__`、属性查找），官方文档。
- round2 表 C（派发机制背景）。
- C++ vtable 一手规范章节待核；机器层落地交叉参 L3-03；Rust trait 派发对照留 L5-14（勿重复立项）。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；实际落盘 `study/cs-kb/findings/011-oop-paradigms-t4-dynamic-dispatch.md`（拆分 `...-a/-b`）。subagent 自 Write，报告自足，不残留工具标签/控制字符。
```

```text
[P1] L3-04·大主题5 接口、抽象与设计原则 — 报告prompt

【任务】为「L3-04·大主题5 接口、抽象与设计原则」产出一份 report-format v3（广度优先教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 4 个小主题（5.1–5.4），偏原则性。先判定"1 份 or 拆 N 份 + 理由"。倾向 1 份；若拆分用 -a/-b。判定与理由写在抬头下。

【v3 格式硬性（必须遵守）】
- 层级 `#`→`##`→`###`；`###` 下不写标签，正文分段：核心说明（正确）+ 充分辅助说明（初学者直觉/为什么/最小例子/易错点/关联，讲到懂、必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲懂不纵深。
- 抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 5」；关键项复述：
- 5.1 抽象基类/协议 — ABC/Protocol 强制接口（Python Data Model abc；未实现即报错）。
- 5.2 里氏替换原则 LSP — 子类型可替换性（Liskov 1987；给违反 LSP 反例）。
- 5.3 组合优于继承 — 脆弱基类问题（委托 vs 继承对照）。
- 5.4 SOLID 原则概览 — 五原则（架构方法学应用深挖归 L4-06，此处只作语言机制层概览）。
广度完备：内容项查全。

【多来源比对（强制）】
- 每项 ≥2 独立源，优先一手（Liskov 1987 论文 / Python 3.11 官方 abc/typing 文档 / 原始 SOLID 出处）；冲突两边都记、点明分歧。
- 不编造原则表述/来源归属；未证实项标「待核」，标核实日期。
- Python/JS 示例可给（abc 未实现报错、LSP 反例、组合 vs 继承），独占块。
- 每个 `##` 章节末集中列「来源与时效」，不逐项脚注。

【权威锚点清单（取自 round3a）】
- Barbara Liskov《Data Abstraction and Hierarchy》(1987)——LSP 源头。
- Python 3.11 Data Model（abc/Protocol），官方文档。
- round2（设计原则背景）；SOLID 方法学应用交叉参 L4-06（勿重复立项）。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；实际落盘 `study/cs-kb/findings/011-oop-paradigms-t5-interfaces-principles.md`（拆分 `...-a/-b`）。subagent 自 Write，报告自足，不残留工具标签/控制字符。
```

```text
[P1] L3-04·大主题6 泛型与参数多态 — 报告prompt

【任务】为「L3-04·大主题6 泛型与参数多态」产出一份 report-format v3（广度优先教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 4 个小主题（6.1–6.4）。先判定"1 份 or 拆 N 份 + 理由"。倾向 1 份；若拆分用 -a/-b。判定与理由写在抬头下。

【v3 格式硬性（必须遵守）】
- 层级 `#`→`##`→`###`；`###` 下不写标签，正文分段：核心说明（正确）+ 充分辅助说明（初学者直觉/为什么/最小例子/易错点/关联，讲到懂、必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲懂不纵深。
- 抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 6」；关键项复述：
- 6.1 多态四分类 — 参数/包含/特设/强制（Cardelli-Wegner 1985）。
- 6.2 参数多态与泛型容器 — 类型参数化（Python `typing.Generic`/`TypeVar`）。
- 6.3 特设多态 — 重载与运算符重载（Python `__add__`/`__eq__`）。
- 6.4 实现策略对比 — 擦除(Java) vs 单态化(C++) vs 结构化鸭子(Python)。
广度完备：内容项查全。

【多来源比对（强制）】
- 每项 ≥2 独立源，优先一手（Cardelli-Wegner 1985 论文 / Java 语言规范或权威文档 / C++ 标准 / Python typing 官方文档）；四分类等术语冲突两边都记、点明分歧。
- 不编造分类边界/实现细节；未证实项标「待核」，标核实日期。
- Python/JS 示例可给，独占块。
- 每个 `##` 章节末集中列「来源与时效」，不逐项脚注。

【权威锚点清单（取自 round3a）】
- Cardelli & Wegner《On Understanding Types, Data Abstraction, and Polymorphism》(1985)——多态四分类源头。
- Python 3.11 typing（Generic/TypeVar）、Data Model（运算符重载协议），官方文档。
- 关联：类型系统形式化归 L5-06；Rust 泛型/trait 约束归 L5-14（勿重复立项）。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；实际落盘 `study/cs-kb/findings/011-oop-paradigms-t6-generics-polymorphism.md`（拆分 `...-a/-b`）。subagent 自 Write，报告自足，不残留工具标签/控制字符。
```

```text
[P1] L3-04·大主题7 函数式范式 — 报告prompt

【任务】为「L3-04·大主题7 函数式范式」产出一份 report-format v3（广度优先教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 5 个小主题（7.1–7.5），FP 核心、内容项密。先判定"1 份 or 拆 N 份 + 理由"。倾向 1 份；若体量偏大可拆 -a/-b。判定与理由写在抬头下。

【v3 格式硬性（必须遵守）】
- 层级 `#`→`##`→`###`；`###` 下不写标签，正文分段：核心说明（正确）+ 充分辅助说明（初学者直觉/为什么/最小例子/易错点/关联，讲到懂、必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲懂不纵深。
- 抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 7」；关键项复述：
- 7.1 一等与高阶函数 — 函数作参数/返回值（SICP §1.3；正式化在此，L1-01 只给基础函数）。
- 7.2 闭包与自由变量捕获 — 晚绑定陷阱（SICP §1.3；CMU 15-150；如循环中 lambda 晚绑定 bug）。
- 7.3 不可变性与纯函数 — 引用透明、无副作用（CMU 15-150；如 frozenset/tuple）。
- 7.4 map/filter/reduce 与序列处理 — 高阶序列变换（SICP §2.2.1；`functools.reduce`）。
- 7.5 副作用隔离 — 纯核心/薄壳、work & span 直觉（CMU 15-150）。
广度完备：内容项查全。

【多来源比对（强制）】
- 每项 ≥2 独立源，优先一手（SICP 章节 / CMU 15-150 syllabus 及讲义）；冲突两边都记、点明分歧。
- 不编造术语定义；未证实项标「待核」，标核实日期。
- Python/JS 示例可给（晚绑定 bug、纯函数重构最直观），独占块。
- 每个 `##` 章节末集中列「来源与时效」，不逐项脚注。

【权威锚点清单（取自 round3a）】
- SICP §1.3（Higher-Order Procedures）、§2.2.1（序列处理），https://sarabander.github.io/sicp/html/index.xhtml 。
- CMU 15-150《Principles of Functional Programming》(Fall 2015, SML)，https://www.cs.cmu.edu/~iliano/courses/15F-CMU-CS150/syllabus.shtml 。
- 关联：λ 演算根归 L5-06（勿深挖）。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；实际落盘 `study/cs-kb/findings/011-oop-paradigms-t7-functional.md`（拆分 `...-a/-b`）。subagent 自 Write，报告自足，不残留工具标签/控制字符。
```

```text
[P1] L3-04·大主题8 状态、可变性与求值模型 — 报告prompt

【任务】为「L3-04·大主题8 状态、可变性与求值模型」产出一份 report-format v3（广度优先教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 4 个小主题（8.1–8.4）。先判定"1 份 or 拆 N 份 + 理由"。倾向 1 份；若拆分用 -a/-b。判定与理由写在抬头下。

【v3 格式硬性（必须遵守）】
- 层级 `#`→`##`→`###`；`###` 下不写标签，正文分段：核心说明（正确）+ 充分辅助说明（初学者直觉/为什么/最小例子/易错点/关联，讲到懂、必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲懂不纵深。
- 抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 8」；关键项复述：
- 8.1 赋值与局部状态的代价 — 引入状态破坏引用透明（SICP §3.1；如计数器闭包）。
- 8.2 环境模型的求值 — 帧/绑定链解释求值（SICP §3.2；环境链手推）。
- 8.3 可变数据与共享风险 — 别名下的意外修改（SICP §3.3；如可变默认参数陷阱）。
- 8.4 流与惰性求值（引申）— 延迟求值、无穷流（SICP §3.5；CMU 15-150；如生成器惰性）。
广度完备：内容项查全。

【多来源比对（强制）】
- 每项 ≥2 独立源，优先一手（SICP 章节 / CMU 15-150 流与记忆化讲义）；冲突两边都记、点明分歧。
- 不编造语义细节；未证实项标「待核」，标核实日期。
- Python/JS 示例可给（可变默认参数陷阱、生成器惰性最直观），独占块。
- 每个 `##` 章节末集中列「来源与时效」，不逐项脚注。

【权威锚点清单（取自 round3a）】
- SICP Ch3（Modularity, Objects, and State：§3.1 赋值与局部状态、§3.2 环境模型、§3.3 可变数据、§3.5 流），https://sarabander.github.io/sicp/html/index.xhtml 。
- CMU 15-150（streams/惰性/记忆化）。
- 关联：可变共享状态与并发深挖归 L4-05（勿重复立项）。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；实际落盘 `study/cs-kb/findings/011-oop-paradigms-t8-state-evaluation.md`（拆分 `...-a/-b`）。subagent 自 Write，报告自足，不残留工具标签/控制字符。
```

```text
[P1] L3-04·大主题9 声明式与元语言抽象（选修广度）— 报告prompt

【任务】为「L3-04·大主题9 声明式与元语言抽象（选修广度）」产出一份 report-format v3（广度优先教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 3 个小主题（9.1–9.3），定位为"选修广度"、深挖归 L5-06。先判定"1 份 or 拆 N 份 + 理由"。几乎必为 1 份（小主题少、只作广度）；若判拆分需给强理由。判定与理由写在抬头下。

【v3 格式硬性（必须遵守）】
- 层级 `#`→`##`→`###`；`###` 下不写标签，正文分段：核心说明（正确）+ 充分辅助说明（初学者直觉/为什么/最小例子/易错点/关联，讲到懂、必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲懂不纵深（本主题尤其只作范式广度，不做深挖）。
- 抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 9」；关键项复述：
- 9.1 元循环求值器视角 — 解释器即抽象（SICP §4.1；如玩具 eval）。
- 9.2 逻辑/关系式编程 — 声明式范式一支（SICP §4.4；微型规则引擎直觉）。
- 9.3 惰性/非确定性求值广度 — 求值策略变体（SICP §4.2-4.3；amb 直觉）。
广度完备：内容项查全，但只作广度概览，机制深挖显式归 L5-06 与 L1-01（CS61A 解释器/SQL）。

【多来源比对（强制）】
- 每项 ≥2 独立源，优先一手（SICP Ch4 章节）；冲突两边都记、点明分歧。
- 不编造求值策略细节；未证实项标「待核」，标核实日期。
- Python 示例可给（玩具 eval、amb 直觉），独占块。
- 每个 `##` 章节末集中列「来源与时效」，不逐项脚注。

【权威锚点清单（取自 round3a）】
- SICP Ch4（Metalinguistic Abstraction：§4.1 元循环求值器、§4.2-4.3 惰性/非确定性、§4.4 逻辑编程），https://sarabander.github.io/sicp/html/index.xhtml 。
- 关联：⚠与 L1-01（CS61A 解释器/SQL）、L5-06 重叠；此处仅范式广度，深挖归 L5-06（勿重复立项）。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；实际落盘 `study/cs-kb/findings/011-oop-paradigms-t9-declarative-metalinguistic.md`（拆分 `...-a/-b`）。subagent 自 Write，报告自足，不残留工具标签/控制字符。
```

```text
[P1] L3-04·大主题10 设计模式作为范式产物（可选）— 报告prompt

【任务】为「L3-04·大主题10 设计模式作为范式产物（可选）」产出一份 report-format v3（广度优先教辅式）报告。先做粒度判定，再落盘。

【粒度判定（动手前先做）】本大主题含 4 个小主题（10.1–10.4），定位为"可选单元"、方法学应用归 L4-06。先判定"1 份 or 拆 N 份 + 理由"。倾向 1 份；若拆分用 -a/-b。判定与理由写在抬头下。

【v3 格式硬性（必须遵守）】
- 层级 `#`→`##`→`###`；`###` 下不写标签，正文分段：核心说明（正确）+ 充分辅助说明（初学者直觉/为什么/最小例子/易错点/关联，讲到懂、必需）。
- 公式独占行；代码片段独占块；教辅口吻；广度优先、讲懂不纵深。
- 抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，标先修、一手锚点、核实日期。

【小主题清单（每个作一个 ## 章节）与应覆盖内容项】
下游可读 `round3b-subtopics/g02-programming-ds.md` 中 L3-04「大主题 10」；关键项复述：
- 10.1 创建型模式 — 工厂/单例（GoF 1994；如工厂函数）。
- 10.2 结构型模式 — 适配器/装饰器（GoF 1994；Python `@decorator`）。
- 10.3 行为型模式 — 策略/观察者（GoF 1994；策略模式）。
- 10.4 FP 视角下的模式消解 — 部分模式退化为高阶函数（策略=传函数对照）。
广度完备：内容项查全；模式为 OO 惯用法结晶，只讲语言层用法，架构方法学应用归 L4-06。

【多来源比对（强制）】
- 每项 ≥2 独立源，优先一手（GoF《Design Patterns》1994 原书 / 官方语言文档）；模式命名/分类冲突两边都记、点明分歧。
- 不编造模式意图/结构；未证实项标「待核」，标核实日期。
- Python/JS 示例可给（装饰器、策略=传函数对照最直观），独占块。
- 每个 `##` 章节末集中列「来源与时效」，不逐项脚注。

【权威锚点清单（取自 round3a）】
- GoF《Design Patterns: Elements of Reusable Object-Oriented Software》(1994)——创建型/结构型/行为型。
- Python 3.11 官方文档（decorator 等语言机制）。
- 关联：SOLID/模式的架构方法学应用归 L4-06（勿重复立项）；FP 消解视角关联大主题7。

【落盘】base = `study/cs-kb/findings/011-oop-paradigms.md`；实际落盘 `study/cs-kb/findings/011-oop-paradigms-t10-design-patterns.md`（拆分 `...-a/-b`）。subagent 自 Write，报告自足，不残留工具标签/控制字符。
```

共 10 条 prompt
