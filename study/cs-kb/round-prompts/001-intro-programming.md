# Round3c · L1-01 程序设计入门 · 报告 prompt（P0）

> 本文件为 L1-01 的正式投喂 prompt 库。L1-01 共 K=8 个大主题，每大主题 1 条报告 prompt，下列 8 条。
> 每条 prompt 自足可直接投喂给一个报告 subagent；下游须先读 `study/cs-kb/report-format.md`（v3 模板）与 `study/cs-kb/brief.md`（纪律）再开工。

```text
[P0] L1-01·大主题1 值、变量与两套心智模型 — 报告prompt

【任务】
为「L1-01·大主题1 值、变量与两套心智模型」产出一份符合 report-format v3 的教辅式广度报告。开工前先读 study/cs-kb/report-format.md（v3 模板，硬约束）与 study/cs-kb/brief.md（纪律）。主题主线：字面量/表达式/类型；C「有类型的内存位置」vs Python「名字绑定对象」；赋值语义与别名陷阱。

【v3 格式硬性（必须逐条遵守）】
- 标题层级固定：`#` 大主题（=报告标题） → `##` 小主题（=章节） → `###` 内容项（每项一节）。不得越级或改层。
- `###` 内容项下不写任何标签（严禁出现「核心概念：」「辅助说明：」等字样）：直接正文、靠空行分段。先一段核心说明（是什么/定义/关键结论，正确），再充分的辅助说明（面向初学者的直觉/为什么/最小例子/易错点/前后关联，写到初学者能懂为止，可多段）。辅助说明是必需项、要充分，不是一句带过的点缀。
- 公式/关键代码骨架独占一行（块级），不内联埋进句子。
- 教辅口吻；广度优先：小主题下内容项查全，宁可多列浅讲；每项讲到初学者懂即止，不做专家级纵深（不写长篇机制深挖/完整推导/设计权衡长论）。
- 抬头须含可 grep 基线串 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，并按 v3 给「基线/核实日期 ｜ 先修 ｜ 一手锚点 ｜ 成熟度（本主题为 GA/稳定）」。

【小主题清单（作 `##` 章节，共 5 个）与应覆盖的内容项】
下游可读 study/cs-kb/round3b-subtopics/g02-programming-ds.md 中 L1-01 大主题1 的 1.1–1.5 原文核对，但以下已自足：
- 1.1 字面量与基本类型 —— 整/浮/字符字面量；C 的静态类型分类（int/char/float/double 等）vs Python 动态类型；各类型大小与取值直觉。
- 1.2 表达式、运算符、优先级与结合性 —— 算术/关系/逻辑/位运算符；优先级与结合性；求值顺序踩坑。
- 1.3 类型转换与提升 —— C 隐式整型提升与常用算术转换、显式强制转换；Python 无隐式数值窄化。
- 1.4 变量与赋值语义 —— C「有类型的内存位置」写入 vs Python「名字绑定对象」；赋值到底改了什么。
- 1.5 别名与身份陷阱 —— Python `is` vs `==`、可变对象共享；C 同一地址多个名字。

【多来源比对强制】
- 每个内容项须用 ≥2 个独立来源交叉核对，优先一手（教材/规范/官方文档）；来源打架时两边都记、点明分歧、不和稀泥。
- 不编造：具体数值/默认值/版本号/公式系数/字长等，来自比对通过的来源，否则标「待核」并注核实日期，绝不凭记忆填。
- 每个 `##` 小主题末集中列「#### 来源与时效」（锚点+版本+核实日期+用到的比对来源+冲突项定位），不逐项脚注。前沿/演进快项硬标「⚙演进快·锚版本·随时变」（本主题多为稳定 GA，一般无此项）。
- 实机验证可选：仅在便宜且能加固某结论时做（如 C 打印各类型 sizeof、Python `id()`/`is` 观察绑定与别名），贴真实输出+命令+基线串；不逐项强制，且不得替代多来源比对。

【权威锚点清单（取自 round3a）】
- Berkeley CS61A《SICP》官方 syllabus + lecture schedule，https://cs61a.org/ （核实 2026-07-25：线上为 Summer 2026 学期，Python 为主）——本主题锚 Environments 讲次（名字绑定/环境模型）。
- K&R《The C Programming Language》2nd ed（1988，基于 C89）——锚 Ch2（类型/运算符/表达式）。
- ISO/IEC 9899:2011 (C11) 标准文本——裁决整型提升/转换/求值顺序细节。
- Python 3.11 官方 Language Reference——数据模型 §3（对象/值/类型/身份）、执行模型 §4（名字绑定）。

【粒度判定（开工先判）】
先判本大主题「1 份 or 拆 N 份 + 理由」（依据：小主题数=5、是否跨机制、篇幅）。默认 1 大主题 = 1 报告；仅当过长/跨机制才拆 `-a/-b`。把判定与理由写进报告开头（或交主 agent 记入 ledger）。

【落盘目标】
findings/001-intro-programming.md（拆则 findings/001-intro-programming-a.md / -b.md）。自己 Write，不得残留工具标签/控制字符/scratchpad 引用。
```

```text
[P0] L1-01·大主题2 控制流与结构化编程 — 报告prompt

【任务】
为「L1-01·大主题2 控制流与结构化编程」产出一份符合 report-format v3 的教辅式广度报告。开工前先读 study/cs-kb/report-format.md（v3）与 study/cs-kb/brief.md。主题主线：分支/循环/短路求值；布尔逻辑；结构化定理；C 的 UB 陷阱（有符号溢出/未初始化读/序列点）。

【v3 格式硬性（必须逐条遵守）】
- 层级固定：`#` 大主题 → `##` 小主题 → `###` 内容项。
- `###` 下不写标签（无「核心概念：/辅助说明：」字样）：先核心说明（是什么/定义/结论，正确），再充分辅助说明（初学者向直觉/为什么/最小例子/易错点/关联，多段亦可，必需且充分）。
- 公式/代码骨架独占一行（块级），不内联。
- 教辅口吻；广度优先、查全宁多列浅讲；每项讲到初学者懂即止，不做专家纵深。
- 抬头含 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25` 及 v3 抬头字段（基线/核实日期｜先修：大主题1｜一手锚点｜成熟度）。

【小主题清单（`##` 章节，共 5 个）与内容项】
可核对 round3b-subtopics/g02-programming-ds.md 之 L1-01 大主题2 的 2.1–2.5，以下已自足：
- 2.1 条件分支 —— if/else、switch/case（C fall-through）、条件（三元）表达式。
- 2.2 循环结构 —— while/for/do-while、循环变量与边界、off-by-one。
- 2.3 短路求值与布尔逻辑 —— C `&&`/`||`；Python `and`/`or` 返回操作数而非 bool、`x or default` 惯用法。
- 2.4 跳转与结构化定理 —— break/continue/goto；结构化 vs 非结构化控制；goto 清理惯用法。
- 2.5 C 未定义行为陷阱 —— 有符号整数溢出、未初始化读、序列点/求值顺序未定义。

【多来源比对强制】
- 每内容项 ≥2 独立来源交叉核对，优先一手；冲突两边都记、点明分歧。
- 不编造数值/默认值/版本号；无据标「待核」+核实日期。
- 每 `##` 末集中列「#### 来源与时效」（锚点+版本+核实日期+比对来源+冲突定位），不逐项脚注；UB 相关须显式引 C11 序列点/求值顺序条款定位（章节号无把握则标「待核」，不臆造条号）。
- 实机验证可选：如 C fall-through、`gcc -fsanitize=undefined` 触发有符号溢出、Python 短路返回操作数——做则贴真实输出+命令+基线串；不替代多来源比对。

【权威锚点清单（取自 round3a）】
- CS61A（Control 讲次），https://cs61a.org/ （核实 2026-07-25，Summer 2026）。
- K&R 2nd ed（C89）Ch3（控制流）。
- ISO/IEC 9899:2011 (C11)——UB/序列点/求值顺序裁决。
- Python 3.11 Language Reference——布尔运算/控制流语义。

【粒度判定（开工先判）】
判「1 份 or 拆 N 份 + 理由」（小主题=5，UB 部分是否需独立成份）。默认 1 份；过长/跨机制才拆 `-a/-b`。判定+理由写报告开头或交 ledger。

【落盘目标】
findings/002-intro-programming.md（拆则 -a/-b）。自己 Write，无残留工具标签/控制字符/scratchpad 引用。
```

```text
[P0] L1-01·大主题3 函数、作用域与调用约定 — 报告prompt

【任务】
为「L1-01·大主题3 函数、作用域与调用约定」产出一份符合 report-format v3 的教辅式广度报告。先读 report-format.md（v3）与 brief.md。主题主线：定义/参数/返回；值传递 vs 引用/指针传递；作用域与环境模型；栈帧建立与销毁。注意跨课边界：栈帧的机器层落地深挖归 L3-03；高阶函数/闭包正式化归 L3-04（本报告只给基础函数与作用域直觉）。

【v3 格式硬性（必须逐条遵守）】
- 层级固定：`#`→`##`→`###`。`###` 下不写标签：先核心说明、再充分辅助说明（初学者向直觉/为什么/最小例子/易错点/关联，必需且充分）。
- 公式/代码骨架独占一行；教辅口吻；广度优先、查全浅讲、不做专家纵深。
- 抬头含 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25` 及 v3 抬头字段（先修：大主题1、大主题2）。

【小主题清单（`##` 章节，共 5 个）与内容项】
可核对 round3b 之 L1-01 大主题3 的 3.1–3.5，已自足：
- 3.1 函数定义、参数与返回值 —— 声明/定义/原型；C 无原型的隐式声明警告。
- 3.2 传值 vs 指针/引用传递 —— C 传值 + 指针模拟引用（swap 需传指针）；Python 传对象引用（可变实参被改）。
- 3.3 作用域与生命周期 —— 局部/全局；C `static`/`extern`；Python LEGB 规则。
- 3.4 环境模型与栈帧 —— 调用时帧的建立与销毁、局部绑定表（机器层落地归 L3-03，本处只给模型直觉）。
- 3.5 多文件程序结构 —— 头文件、声明与定义分离、链接（分离编译）。

【多来源比对强制】
- 每内容项 ≥2 独立来源交叉核对，优先一手；冲突两边都记。
- 不编造；无据标「待核」+核实日期。每 `##` 末集中列「#### 来源与时效」。
- 实机验证可选：C swap 传指针、Python 可变实参被改、LEGB/闭包观察、`gcc` 分离编译+链接——做则贴真实输出+命令+基线串；不替代多来源比对。

【权威锚点清单（取自 round3a）】
- CS61A（Functions、Environments 讲次），https://cs61a.org/ （核实 2026-07-25）。
- K&R 2nd ed（C89）Ch4（函数与程序结构）。
- Python 3.11 Language Reference——执行模型 §4（名字绑定与作用域/LEGB）。
- ISO/IEC 9899:2011 (C11)——链接与存储期（static/extern）语义。

【粒度判定（开工先判）】
判「1 份 or 拆 N 份 + 理由」（小主题=5）。默认 1 份。判定+理由写开头或交 ledger。

【落盘目标】
findings/003-intro-programming.md（拆则 -a/-b）。自己 Write，无残留工具标签/控制字符/scratchpad 引用。
```

```text
[P0] L1-01·大主题4 递归与栈 — 报告prompt

【任务】
为「L1-01·大主题4 递归与栈」产出一份符合 report-format v3 的教辅式广度报告。先读 report-format.md（v3）与 brief.md。主题主线：基例/递归例；递归=编译器管理的调用栈；树递归；尾递归；两种失败面（C→SIGSEGV / Python→RecursionError）。跨课边界：调用栈/树作为数据结构的深挖归 L2-01。

【v3 格式硬性（必须逐条遵守）】
- 层级固定 `#`→`##`→`###`；`###` 下不写标签，先核心说明再充分辅助说明（必需且充分，初学者向）。
- 公式独占行（如递归式 T(n)=T(n-1)+O(1) 之类单独成行）；教辅口吻；广度优先、查全浅讲、不做专家纵深。
- 抬头含 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25` 及 v3 抬头字段（先修：大主题3）。

【小主题清单（`##` 章节，共 5 个）与内容项】
可核对 round3b 之 L1-01 大主题4 的 4.1–4.5，已自足：
- 4.1 基例与递归例 —— 递归分解与归纳正确性直觉（阶乘/求和）。
- 4.2 递归=编译器管理的调用栈 —— 栈帧压入/展开。
- 4.3 树递归与重叠子问题 —— 朴素斐波那契的指数级爆炸、调用次数计数。
- 4.4 尾递归与迭代等价 —— 尾调用改写为循环；递归↔迭代对照。
- 4.5 两种失败面 —— C 栈溢出→SIGSEGV / Python→RecursionError（`sys.setrecursionlimit`）。

【多来源比对强制】
- 每内容项 ≥2 独立来源交叉核对，优先一手；冲突两边都记。
- 不编造递归深度默认值/极限等：如 Python 默认递归上限，须比对官方来源，无把握标「待核」+核实日期。
- 每 `##` 末集中列「#### 来源与时效」。
- 实机验证可选：Python 计数调用次数看指数爆炸、`sys.setrecursionlimit` 触发 RecursionError、C 无限递归 segfault + gdb backtrace——做则贴真实输出+命令+基线串；不替代多来源比对。

【权威锚点清单（取自 round3a）】
- CS61A（Recursion、Tree Recursion 讲次），https://cs61a.org/ （核实 2026-07-25）。
- K&R 2nd ed（C89）——递归函数示例。
- Python 3.11 官方文档——sys.setrecursionlimit / RecursionError（数值须比对，不臆造）。
- ISO/IEC 9899:2011 (C11)——递归调用与自动存储期。

【粒度判定（开工先判）】
判「1 份 or 拆 N 份 + 理由」（小主题=5）。默认 1 份。判定+理由写开头或交 ledger。

【落盘目标】
findings/004-intro-programming.md（拆则 -a/-b）。自己 Write，无残留工具标签/控制字符/scratchpad 引用。
```

```text
[P0] L1-01·大主题5 复合数据与基础数据结构入门 — 报告prompt

【任务】
为「L1-01·大主题5 复合数据与基础数据结构入门」产出一份符合 report-format v3 的教辅式广度报告。先读 report-format.md（v3）与 brief.md。主题主线：数组/字符串；Python list/tuple/set/dict；C 结构体与数组；可变数据与数据抽象入门。★重要跨课边界：本大主题是 L1-01↔L2-01 接缝——结构的实现深挖（动态数组扩容、链表/哈希/树的实现与代价画像）显式下沉归 L2-01；本报告只给「复合数据的用法与数据抽象雏形」入门直觉，不做结构实现深挖。

【v3 格式硬性（必须逐条遵守）】
- 层级固定 `#`→`##`→`###`；`###` 下不写标签，先核心说明再充分辅助说明（必需且充分，初学者向）。
- 公式/代码骨架独占行；教辅口吻；广度优先、查全浅讲、不做专家纵深。
- 抬头含 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25` 及 v3 抬头字段（先修：大主题1、大主题3；成熟度 GA），并显式写明与 L2-01 的下沉边界。

【小主题清单（`##` 章节，共 5 个）与内容项】
可核对 round3b 之 L1-01 大主题5 的 5.1–5.5，已自足：
- 5.1 数组与 C 字符串 —— 定长数组、`'\0'` 结尾字符串、字符数组遍历。
- 5.2 Python 序列容器 —— list/tuple/range、索引与切片语义。
- 5.3 集合与映射入门 —— set/dict 的构造与用法、dict 键去重。
- 5.4 C 结构体与聚合数据 —— `struct` 定义、成员访问、内存对齐直觉（对齐填充只给直觉，深挖归 L3-02/L2-01）。
- 5.5 可变性与数据抽象入门 —— 构造器/选择器、抽象屏障雏形、可变 vs 不可变容器对照。

【多来源比对强制】
- 每内容项 ≥2 独立来源交叉核对，优先一手；冲突两边都记。
- 不编造 sizeof/对齐值：如 `sizeof(struct)` 与对齐填充依赖 ABI，须实测或比对来源，无把握标「待核」+核实日期（本 KB ABI = x86-64 System V）。
- 每 `##` 末集中列「#### 来源与时效」。
- 实机验证可选：Python 切片语义、dict 键去重、可变 vs 不可变对照、C 字符数组遍历、`sizeof(struct)` 观察对齐——做则贴真实输出+命令+基线串；不替代多来源比对。

【权威锚点清单（取自 round3a）】
- CS61A（Sequences、Mutability、Data Abstraction 讲次），https://cs61a.org/ （核实 2026-07-25）。
- K&R 2nd ed（C89）Ch6（结构体）；数组/字符串见 Ch5/Ch1。
- Python 3.11 官方文档——序列/集合/映射类型（数据模型 §3）。
- ISO/IEC 9899:2011 (C11)——结构体/对齐语义。

【粒度判定（开工先判）】
判「1 份 or 拆 N 份 + 理由」（小主题=5，接缝主题；若数据抽象与容器用法篇幅悬殊可考虑拆）。默认 1 份。判定+理由写开头或交 ledger。

【落盘目标】
findings/005-intro-programming.md（拆则 -a/-b）。自己 Write，无残留工具标签/控制字符/scratchpad 引用。
```

```text
[P0] L1-01·大主题6 指针与内存（C 专属地基） — 报告prompt

【任务】
为「L1-01·大主题6 指针与内存（C 专属地基）」产出一份符合 report-format v3 的教辅式广度报告。先读 report-format.md（v3）与 brief.md。主题主线：地址算术/sizeof/指针与数组/多级指针/动态分配 malloc-free/悬垂与野指针。本大主题为 C 专属地基。跨课边界：用指针造的链表实现归 L2-01；虚拟内存归 L4-01——本报告只给指针与手动内存管理的语言级机制与陷阱。

【v3 格式硬性（必须逐条遵守）】
- 层级固定 `#`→`##`→`###`；`###` 下不写标签，先核心说明再充分辅助说明（必需且充分，初学者向直觉/为什么/最小例子/易错点）。
- 公式/代码骨架独占行（如 `*(a+i)` 等价关系单独成行说明）；教辅口吻；广度优先、查全浅讲、不做专家纵深。
- 抬头含 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25` 及 v3 抬头字段（先修：大主题5；ABI = x86-64 System V）。

【小主题清单（`##` 章节，共 6 个）与内容项】
可核对 round3b 之 L1-01 大主题6 的 6.1–6.6，已自足：
- 6.1 地址与指针基础 —— `&`/`*`、指针类型与解引用。
- 6.2 指针与数组 —— 等价与差异、数组名退化为指针、`a[i]` vs `*(a+i)`。
- 6.3 指针算术与 sizeof —— 步长按指向类型缩放、`p+1` 地址增量。
- 6.4 多级指针、指针数组与 argv —— 二级指针、命令行参数遍历。
- 6.5 动态内存管理 —— malloc/calloc/realloc/free 的生命周期。
- 6.6 悬垂/野指针、越界、泄漏陷阱 —— 释放后使用、缓冲越界、内存泄漏。

【多来源比对强制】
- 每内容项 ≥2 独立来源交叉核对，优先一手；冲突两边都记。
- 不编造：指针步长/地址增量依赖类型与 ABI，须实测或比对来源；malloc 失败返回/free 语义按标准比对；无把握标「待核」+核实日期。UB 类陷阱须引 C11 相应语义定位（条号无把握则标待核，不臆造）。
- 每 `##` 末集中列「#### 来源与时效」。
- 实机验证可选：C 打印地址/解引用、`p+1` 地址增量、遍历 argv、valgrind 查泄漏、`gcc -fsanitize=address` 触发越界/释放后使用——做则贴真实输出+命令+基线串；不替代多来源比对。

【权威锚点清单（取自 round3a）】
- K&R 2nd ed（C89）Ch5（指针与数组）——本主题主锚。
- ISO/IEC 9899:2011 (C11)——指针算术、对象生命周期、UB 裁决。
- CS61A 不覆盖指针（C 专属）；如需对照可提 Python 无显式指针的对比直觉。

【粒度判定（开工先判）】
判「1 份 or 拆 N 份 + 理由」——本大主题小主题=6（8 个大主题里最多），指针机制 + 动态内存 + 陷阱三块，是最可能需要拆的候选；请据篇幅与是否跨机制给明确理由。默认先按 1 份评估，过长/跨机制则拆 `-a`（6.1–6.4 指针机制）/`-b`（6.5–6.6 动态内存与陷阱）。判定+理由写开头或交 ledger。

【落盘目标】
findings/006-intro-programming.md（拆则 findings/006-intro-programming-a.md / -b.md）。自己 Write，无残留工具标签/控制字符/scratchpad 引用。
```

```text
[P0] L1-01·大主题7 简单 I/O 与类型表示 — 报告prompt

【任务】
为「L1-01·大主题7 简单 I/O 与类型表示」产出一份符合 report-format v3 的教辅式广度报告。先读 report-format.md（v3）与 brief.md。主题主线：printf/scanf 格式串与类型对齐；标准输入输出；整型/浮点表示的入门直觉。跨课边界：表示与内存层次的深挖归 L3-02（组成原理）——本报告只给「表示的入门直觉」，不做位级纵深。

【v3 格式硬性（必须逐条遵守）】
- 层级固定 `#`→`##`→`###`；`###` 下不写标签，先核心说明再充分辅助说明（必需且充分，初学者向）。
- 公式/格式串骨架独占行；教辅口吻；广度优先、查全浅讲、不做专家纵深。
- 抬头含 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25` 及 v3 抬头字段（先修：大主题1、大主题6）。

【小主题清单（`##` 章节，共 5 个）与内容项】
可核对 round3b 之 L1-01 大主题7 的 7.1–7.5，已自足：
- 7.1 格式化输出 printf —— 格式串与实参类型对齐、类型不匹配的 UB。
- 7.2 格式化输入 scanf 与陷阱 —— 转换说明、`&` 遗漏、返回值检查。
- 7.3 标准流与字符 I/O —— stdin/stdout/stderr、getchar/putchar、管道重定向。
- 7.4 整型表示入门 —— 位宽/有无符号/补码直觉、`printf %x` 看位模式。
- 7.5 浮点表示入门直觉 —— IEEE 754 概念直觉（★深挖归 L3-02；且 K&R/C11 的确切章节定位为待核项，须硬标「待核」不臆造章节）。

【多来源比对强制】
- 每内容项 ≥2 独立来源交叉核对，优先一手；冲突两边都记。
- 不编造格式说明符↔类型对应表、补码位模式、IEEE 754 字段宽度：数值须实测或比对来源，无把握标「待核」+核实日期。
- 7.5 浮点章节定位为已知待核项，必须硬标，不得凭记忆填 K&R/C11 章节号。
- 每 `##` 末集中列「#### 来源与时效」。
- 实机验证可选：C 格式串/类型不匹配观察、scanf 读入失败返回值、管道重定向 I/O、`printf %x` 看补码、Python `struct.pack('f',...)` 看浮点字节——做则贴真实输出+命令+基线串；不替代多来源比对。

【权威锚点清单（取自 round3a）】
- K&R 2nd ed（C89）Ch7（输入输出）——本主题主锚。
- ISO/IEC 9899:2011 (C11)——printf/scanf 转换说明与 UB 裁决。
- IEEE 754（浮点表示概念，深挖归 L3-02；此处仅入门直觉）。
- CS61A 不覆盖 C I/O；Python 侧可用官方 struct 文档作字节表示对照。

【粒度判定（开工先判）】
判「1 份 or 拆 N 份 + 理由」（小主题=5，I/O 与「类型表示入门」两块主题略异）。默认 1 份；若表示部分与 I/O 篇幅悬殊可拆。判定+理由写开头或交 ledger。

【落盘目标】
findings/007-intro-programming.md（拆则 -a/-b）。自己 Write，无残留工具标签/控制字符/scratchpad 引用。
```

```text
[P0] L1-01·大主题8 程序正确性与调试基础 — 报告prompt

【任务】
为「L1-01·大主题8 程序正确性与调试基础」产出一份符合 report-format v3 的教辅式广度报告。先读 report-format.md（v3）与 brief.md。主题主线：测试/断言、前后置条件与循环不变式（契约式）、基础调试；效率/增长阶入门直觉。跨课边界：渐近分析的正式化归 L2-01/L3-01——本报告只给「效率与增长阶的入门直觉」，不做 Big-O 形式化。

【v3 格式硬性（必须逐条遵守）】
- 层级固定 `#`→`##`→`###`；`###` 下不写标签，先核心说明再充分辅助说明（必需且充分，初学者向直觉/为什么/最小例子/易错点）。
- 公式独占行（如循环不变式的建立/保持/终止三段论、粗略增长阶记法单独成行）；教辅口吻；广度优先、查全浅讲、不做专家纵深。
- 抬头含 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25` 及 v3 抬头字段（先修：大主题3、大主题4）。

【小主题清单（`##` 章节，共 5 个）与内容项】
可核对 round3b 之 L1-01 大主题8 的 8.1–8.5，已自足：
- 8.1 测试与断言 —— `assert` 用法与失败语义（C `assert` 触发 abort；Python `assert`）。
- 8.2 前置/后置条件（契约式）—— 函数契约表达（以 CMU 15-122/C0 契约注解为锚）。
- 8.3 循环不变式与终止性 —— 不变式的建立/保持/终止论证、断言插桩。
- 8.4 基础调试工具 —— 打印调试、gdb 断点/单步、pdb。
- 8.5 效率与增长阶入门直觉 —— 计数操作、粗略增长阶（正式化归 L2-01/L3-01；本处只给直觉与 timeit 规模曲线直觉）。

【多来源比对强制】
- 每内容项 ≥2 独立来源交叉核对，优先一手；冲突两边都记。
- 不编造：assert 在 NDEBUG 下的行为、C0 契约注解语法等须比对来源，无把握标「待核」+核实日期。
- 每 `##` 末集中列「#### 来源与时效」。
- 实机验证可选：C/Python `assert` 触发、不变式断言插桩、gdb 断点/单步、pdb、`timeit` 测规模曲线——做则贴真实输出+命令+基线串；不替代多来源比对。

【权威锚点清单（取自 round3a）】
- CMU 15-122（C0，强调契约/不变式）——契约式正确性单元的主锚（前置/后置条件、循环不变式）。
- CS61A（Efficiency 讲次），https://cs61a.org/ （核实 2026-07-25）——效率/增长阶入门直觉。
- ISO/IEC 9899:2011 (C11) 与 Python 3.11 官方文档——assert 语义（C `<assert.h>` / Python `assert` 与 `__debug__`/`-O`）。

【粒度判定（开工先判）】
判「1 份 or 拆 N 份 + 理由」（小主题=5）。默认 1 份。判定+理由写开头或交 ledger。

【落盘目标】
findings/008-intro-programming.md（拆则 -a/-b）。自己 Write，无残留工具标签/控制字符/scratchpad 引用。
```

共 8 条 prompt
