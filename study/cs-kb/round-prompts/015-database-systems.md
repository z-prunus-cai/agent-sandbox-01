# Round3c · L4-03 数据库系统 · 报告 prompt（P0）

```text
[P0] L4-03·大主题03-1 关系模型与关系代数 — 报告prompt
任务：为「L4-03·大主题03-1 关系模型与关系代数」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（若判定拆分，见文末粒度判定，用 015-database-systems-<slug>-a/-b）。

【v3 格式硬性（照 report-format.md v3）】
- 标题层级固定：`#` 大主题（报告标题）→ `##` 小主题（章节）→ `###` 每个内容项一节；`###` 之下不写「核心概念：」「辅助说明：」等任何标签，直接正文靠空行分段。
- 每个 `###` 内：先一段核心说明（是什么/定义/关键结论，正确），再充分辅助说明（面向初学者把它讲懂所需：直觉/为什么这样/最小例子/易错点/与前后关联，可多段；这是必需项，不是点缀）。
- 任何 SQL 语句、关系代数表达式（σ/π/⋈/ρ 等）、公式一律独占行（块级），不内联埋进句子。
- 教辅口吻；广度优先：小主题下的内容项要查全、宁多列浅讲不遗漏；每项讲到初学者懂即止，不做专家级纵深（不写长篇机制深挖/推导/设计权衡长论）。
- 抬头带可 grep 基线串：`基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`，并注明先修/一手锚点/核实日期。

【小主题清单（=`##` 章节）与每小主题应覆盖内容项】
下游可读 round3b-subtopics/g07-data-management.md 中 L4-03「03-1」小节取全部内容项，以下为骨架（小主题 03-1.1~03-1.5，务必查全）：
- 03-1.1 关系结构：关系/元组/属性/域、schema vs instance、关系的集合语义。
- 03-1.2 键：超键/候选键/主键/外键与参照完整性约束。
- 03-1.3 关系代数基本运算：选择 σ、投影 π、并、差、笛卡尔积、改名 ρ。
- 03-1.4 扩展运算：各类连接（自然/θ/外）、交、除、赋值与广义投影。
- 03-1.5 表达能力等价：关系代数 ↔ 元组/域关系演算（安全性）。

【多来源比对强制】
- 每个内容项 ≥2 个独立来源交叉核对，优先一手：DBSC 7e ch2、CMU 15-445 Sp2026 L1；术语/记号如有分歧再引 SQL 标准（ISO/IEC 9075）措辞。
- 规范 vs 实现分歧显式记录：如关系的「集合语义」在标准里去重、而 SQL 实现（SQLite/Postgres）默认多集（bag）语义、需 DISTINCT——两边都记、点明分歧、不和稀泥。
- 冲突项两边都写、各自定位来源。数值/默认值/版本不确定标「待核」，不编造，标核实日期。
- 本机实证可选（非强制）：SQLite/Postgres 用小表逐一跑代数算子对应 SQL、核对结果集；做了就贴命令+真实输出+基线串。
- 每个 `##` 章节末集中列「来源与时效」（锚点+版本+核实日期+冲突项定位），不逐项脚注。

【权威锚点清单】
- 《Database System Concepts》7th ed（Silberschatz/Korth/Sudarshan, 2019）https://www.db-book.com/ — ch2。
- CMU 15-445 Intro to DB, Spring 2026 https://15445.courses.cs.cmu.edu/spring2026/schedule.html — L1。
- SQL 标准 ISO/IEC 9075（规范措辞比对，规范 vs 实现分账）；实现侧 SQLite / PostgreSQL 官方文档。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：本大主题 5 个小主题、单一机制（关系模型地基），默认倾向 1 份；若展开后过长再拆并说明。判定写入报告工作记录/ledger。
```

```text
[P0] L4-03·大主题03-2 SQL 语言（基础→高级） — 报告prompt
任务：为「L4-03·大主题03-2 SQL 语言（基础→高级）」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（若判定拆分，见文末粒度判定，用 015-database-systems-<slug>-a/-b）。

【v3 格式硬性（照 report-format.md v3）】
- 标题层级固定：`#`→`##`→`###`；`###` 之下不写任何标签，直接正文靠空行分段。
- 每个 `###` 内：核心说明（是什么/定义/关键结论，正确）+ 充分辅助说明（面向初学者：直觉/为什么/最小例子/易错点/关联，可多段，必需）。
- 任何 SQL 语句/公式独占行（块级），不内联埋句。
- 教辅口吻；广度优先查全内容项；每项讲到初学者懂即止，不做专家纵深。
- 抬头带基线串 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25` + 先修/一手锚点/核实日期。

【小主题清单（=`##` 章节）与每小主题应覆盖内容项】
下游可读 round3b-subtopics/g07-data-management.md 中 L4-03「03-2」小节取全部内容项（小主题 03-2.1~03-2.6，务必查全）：
- 03-2.1 DDL 与类型/约束：CREATE/ALTER、主外键/CHECK/唯一约束。
- 03-2.2 基础 DML：SELECT-FROM-WHERE、集合运算、DISTINCT/排序。
- 03-2.3 连接与聚合：join 类型、GROUP BY/HAVING、聚合函数。
- 03-2.4 子查询与 CTE：嵌套/相关子查询、EXISTS/IN、WITH。
- 03-2.5 NULL 与三值逻辑：unknown 传播、聚合忽略 NULL、IS NULL。
- 03-2.6 视图/触发器/授权/窗口函数：视图可更新性、触发器语义、GRANT、OVER()。

【多来源比对强制】
- 每项 ≥2 独立源交叉核对，优先一手：DBSC 7e ch3-5、CMU 15-445 Sp2026 L2；语法/语义分歧引 SQL 标准 ISO/IEC 9075 措辞。
- 规范 vs 实现分歧显式：如 NULL 三值逻辑、DISTINCT 语义、窗口函数默认帧、视图可更新性——SQLite 与 PostgreSQL 行为常与标准/彼此不同，两边都记、点明、不和稀泥。
- 冲突两边都写并各自定位；未证实默认值/版本标「待核」，不编造，标核实日期。
- 本机实证可选：SQLite（sqlite3 :memory:）跑三值逻辑与 CTE/窗口函数边界用例；做了贴命令+真实输出+基线串。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch3-5（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L2（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- SQL 标准 ISO/IEC 9075；实现侧 SQLite / PostgreSQL 官方文档（规范 vs 实现分账主力章节）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：6 个小主题、内容项密集（含高级窗口/触发器），若基础 DML 与高级特性合并过长，可考虑拆 -a（DDL/DML/连接聚合）/-b（子查询/NULL/视图触发器窗口）并说明理由；否则 1 份。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-3 数据库设计与 E-R 模型 — 报告prompt
任务：为「L4-03·大主题03-3 数据库设计与 E-R 模型」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；SQL/关系模式转换记号/图独占行/块；教辅口吻；广度优先查全、讲到初学者懂但不做专家纵深；抬头带 `基线 Py3.11.15/np2.4.6/gcc13.3 @2026-07-25`+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b-subtopics/g07-data-management.md 中 L4-03「03-3」小节取全部内容项（03-3.1~03-3.5，查全）：
- 03-3.1 E-R 基本构件：实体集、属性（简单/复合/多值/派生）、联系集。
- 03-3.2 约束：映射基数（1:1/1:N/M:N）、参与约束、键约束。
- 03-3.3 高级建模：弱实体集、泛化/特化/聚集、角色。
- 03-3.4 E-R 图 → 关系模式的转换规则（强/弱实体、联系表消除）。
- 03-3.5 建模陷阱与设计取舍：实体 vs 属性 vs 联系的选择（round3b 标此细目「待核」，务必据一手核实或如实留白）。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch6；E-R 记号存在 Chen 记号 vs Crow's-foot vs UML 类图变体的分歧——显式记两边、点明差异（本课以 DBSC 记号为准并注明其它常见记号存在）。
- 规范 vs 实现：E-R 是概念建模、无单一标准实现；转换到关系模式时不同教材/工具规则细节有出入，冲突两边都记。
- 未证实细目（如 03-3.5）标「待核」，不编造，标核实日期。
- 本机实证不适用（概念建模），如做小型 schema 落地演示可选并注明。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch6（https://www.db-book.com/）为主锚（本大主题为本组独占，内核/架构课不讲概念建模）。
- 次级：E-R 记号对比可引权威教材/官方教学材料佐证（标为交叉验证用，非承重）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：5 个小主题、单一概念建模主题、篇幅适中，默认 1 份。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-4 范式与模式设计 — 报告prompt
任务：为「L4-03·大主题03-4 范式与模式设计」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；函数依赖记号/属性闭包/分解算法等公式独占行/块；教辅口吻；广度优先查全、讲到初学者懂不做专家纵深；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-4」小节取全部内容项（03-4.1~03-4.6，查全）：
- 03-4.1 函数依赖：FD 定义、Armstrong 公理、属性闭包、候选键推导。
- 03-4.2 分解目标：无损连接分解、依赖保持。
- 03-4.3 1NF/2NF/3NF 定义与判定。
- 03-4.4 BCNF 与 3NF 分解算法及二者取舍。
- 03-4.5 多值依赖与 4NF（更高范式 PJNF/5NF 点到）。
- 03-4.6 反范式与工程权衡：冗余换读性能的场景。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch7；BCNF vs 3NF 的「无损 + 依赖保持」可达性结论须交叉核对（BCNF 分解可能不保依赖、3NF 分解可保——两处结论都要核对并记来源）。
- 规范 vs 实现：范式为理论定义、无引擎实现分歧；但「反范式」在工程实践中的取舍属经验，标为工程权衡、注明来源层级。
- 公式/算法（属性闭包、3NF 合成算法、BCNF 分解）不凭记忆写，据一手核对；不确定标「待核」，标核实日期。
- 本机实证可选（理论课口径）：可用 Python/sympy 辅助校验属性闭包或候选键推导的小例子，做了贴脚本+真实输出+基线串（非强制）。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch7（https://www.db-book.com/）为主锚（本组独占）。
- 次级交叉：可引另一权威教材佐证 BCNF/3NF 可达性结论（标非承重）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：6 个小主题、理论密度较高但单一主题，默认 1 份；若 FD/分解算法展开过长可拆 -a（FD/分解目标/低阶范式）/-b（BCNF/MVD-4NF/反范式）并说明。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-5 物理存储与文件组织 — 报告prompt
任务：为「L4-03·大主题03-5 物理存储与文件组织」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；代价模型公式/页布局图/SQL 独占行/块；教辅口吻；广度优先查全、讲到初学者懂不做专家纵深；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-5」小节取全部内容项（03-5.1~03-5.5，查全）；边界：只到行存与缓冲管理，列存/PAX/压缩留 L6-04 §04-2，报告须显式点出此边界：
- 03-5.1 存储层次与介质：磁盘/SSD/RAID、随机 vs 顺序访问代价模型。
- 03-5.2 缓冲区管理：buffer pool、替换策略（LRU/Clock）、pin/dirty、steal/force。
- 03-5.3 记录组织：定长/变长记录、slotted page、大对象（TOAST/溢出页）。
- 03-5.4 页与文件组织：heap file、空闲空间管理、页内布局。
- 03-5.5 元组存储模型：行存（NSM）默认布局（列存/PAX 留 L6-04 §04-2）。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch12-13、CMU 15-445 Sp2026 L3-6。
- 规范 vs 实现分歧显式：slotted page 布局、大对象存储（PostgreSQL TOAST vs SQLite 溢出页/overflow）、默认 page_size——SQLite 与 PostgreSQL 具体机制不同，两边都记、点明、不和稀泥。
- 具体默认值（页大小等）不凭记忆，据官方文档核实或标「待核」，标核实日期。
- 本机实证可选：SQLite `PRAGMA page_size`/`dbstat` 虚表；Postgres `pageinspect` 看页/元组布局；做了贴命令+真实输出+基线串。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch12-13（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L3-6（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- 实现侧 SQLite / PostgreSQL 官方文档（TOAST/溢出页/页大小 规范 vs 实现分账）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：5 个小主题、单一存储层主题，默认 1 份。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-6 索引与 B+树 — 报告prompt
任务：为「L4-03·大主题03-6 索引与 B+树」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；B+树结构图/fanout·树高公式/SQL/EXPLAIN 输出一律独占行/块，不内联；教辅口吻；广度优先查全、讲到初学者懂不做专家纵深；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-6」小节取全部内容项（03-6.1~03-6.6，查全）；边界：教材级原理，LSM/列存索引权衡留 L6-04，负载选型留 L6-09，报告显式点边界：
- 03-6.1 索引基本概念：稠密/稀疏、有序索引 vs 散列、聚簇顺序。
- 03-6.2 B+树结构与操作：查找/插入/删除、分裂合并、fanout 与树高。
- 03-6.3 聚簇 vs 二级索引、覆盖索引/包含列、回表。
- 03-6.4 复合/多属性索引、部分索引、表达式索引、前缀匹配。
- 03-6.5 散列索引与静态/动态散列（可扩展/线性散列）。
- 03-6.6 读执行计划：EXPLAIN 中索引扫描 vs 全表扫描的判读。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch14、CMU 15-445 Sp2026 L7-10。
- 规范 vs 实现分歧显式：聚簇索引（InnoDB 主键聚簇 vs PostgreSQL heap 无天然聚簇/CLUSTER 一次性）、覆盖索引/包含列（PostgreSQL INCLUDE vs SQLite 覆盖）、EXPLAIN 输出格式（SQLite `EXPLAIN QUERY PLAN` vs Postgres `EXPLAIN`）——两边都记、点明、不和稀泥。
- B+树 fanout/树高等结论据一手核对；不确定标「待核」，标核实日期。
- 本机实证可选：SQLite `EXPLAIN QUERY PLAN`；Postgres `EXPLAIN (ANALYZE, BUFFERS)` 观察索引选择与回表；做了贴命令+真实输出+基线串。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch14（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L7-10（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- 实现侧 SQLite / PostgreSQL / InnoDB 官方文档（聚簇/覆盖/EXPLAIN 规范 vs 实现分账）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：6 个小主题、含 B+树结构+散列+计划判读，内容较重，默认 1 份；若过长可拆 -a（概念/B+树结构/聚簇二级覆盖）/-b（复合部分索引/散列/EXPLAIN）并说明。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-7 查询处理与执行 — 报告prompt
任务：为「L4-03·大主题03-7 查询处理与执行」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；代价公式（外部归并趟数/连接代价）、SQL、EXPLAIN 输出独占行/块；教辅口吻；广度优先查全、讲到初学者懂不做专家纵深；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-7」小节取全部内容项（03-7.1~03-7.6，查全）；边界：向量化/JIT/并行执行留 L6-04，报告显式点边界：
- 03-7.1 查询处理流水线：解析→翻译→优化→执行、代价度量单位。
- 03-7.2 选择运算实现：线性扫描 vs 索引扫描的代价。
- 03-7.3 排序：外部归并排序、多路归并与代价公式。
- 03-7.4 连接算法：nested-loop/block/index/sort-merge/hash join。
- 03-7.5 其它运算：去重、聚合、集合运算、投影的实现。
- 03-7.6 执行模型：火山（迭代器）模型、物化 vs 流水线。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch15、CMU 15-445 Sp2026 L11-14。
- 规范 vs 实现分歧显式：连接算法可用性（SQLite 主要 nested-loop，PostgreSQL 有 nested-loop/merge/hash join 三种，可用 `enable_hashjoin` 等开关切换）——两边都记、点明。
- 代价公式（外部归并趟数 ⌈log_{b-1}(N/b)⌉+1 等）不凭记忆，据一手核对；不确定标「待核」，标核实日期。
- 本机实证可选：Postgres `EXPLAIN` 观察不同表规模下 join 算法切换（关 `enable_hashjoin` 对比）；做了贴命令+真实输出+基线串。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch15（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L11-14（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- 实现侧 SQLite / PostgreSQL 官方文档（连接算法/执行模型 规范 vs 实现分账）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：6 个小主题、单一执行层主题，默认 1 份。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-8 查询优化 — 报告prompt
任务：为「L4-03·大主题03-8 查询优化」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；关系代数重写记号、选择率/基数估计公式、SQL/EXPLAIN 输出独占行/块；教辅口吻；广度优先查全、讲到初学者懂不做专家纵深；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-8」小节取全部内容项（03-8.1~03-8.5，查全）；边界：优化器实现内幕（Cascades/morsel）留 L6-04，报告显式点边界：
- 03-8.1 等价变换规则：关系代数表达式重写（谓词下推等）。
- 03-8.2 代价模型基础：I/O 与 CPU 代价、中间结果规模估计。
- 03-8.3 统计信息与基数估计：直方图、选择率、相关性假设。
- 03-8.4 连接顺序枚举：System R 动态规划、左深树、搜索空间。
- 03-8.5 统计维护对计划的影响：ANALYZE、过时统计、优化器提示。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch16、CMU 15-445 Sp2026 L15-16。
- 规范 vs 实现分歧显式：统计维护（PostgreSQL ANALYZE/`pg_stats` 直方图 vs SQLite `ANALYZE`/`sqlite_stat1`）、优化器提示（PostgreSQL 无官方 hint、靠 enable_* 开关；其它引擎有 hint）——两边都记、点明。
- 选择率/基数估计公式、System R DP 复杂度等据一手核对；不确定标「待核」，标核实日期。
- 本机实证可选：Postgres `ANALYZE` 前后 `EXPLAIN` 计划对比、查 `pg_stats` 直方图；做了贴命令+真实输出+基线串。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch16（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L15-16（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- 实现侧 PostgreSQL / SQLite 官方文档（统计/优化器提示 规范 vs 实现分账）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：5 个小主题、单一优化主题，默认 1 份。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-9 事务与 ACID — 报告prompt
任务：为「L4-03·大主题03-9 事务与 ACID」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；调度/优先图记号、SQL（SET TRANSACTION ISOLATION LEVEL）独占行/块；教辅口吻；广度优先查全、讲到初学者懂不做专家纵深；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-9」小节取全部内容项（03-9.1~03-9.5，查全）；边界：分布式事务留 L6-04、跨副本一致性留 L6-09，报告显式点边界：
- 03-9.1 事务概念与 ACID 语义、事务状态机。
- 03-9.2 可串行化：冲突可串行化、视图可串行化、优先图判定。
- 03-9.3 可恢复性与无级联调度。
- 03-9.4 隔离级别谱：RU/RC/RR/Serializable（SQL 标准定义）。
- 03-9.5 异常现象：脏读/不可重复读/幻读/写偏斜（现象↔级别映射）。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch17、CMU 15-445 Sp2026 L17；隔离级别与异常的规范定义须锚 SQL 标准 ISO/IEC 9075。
- 规范 vs 实现分歧显式（本大主题重点）：SQL 标准四级隔离与「三种异常」定义 vs 实际实现——PostgreSQL 无真正 READ UNCOMMITTED（映射为 READ COMMITTED）、REPEATABLE READ 用快照隔离且能防幻读、写偏斜是 SI 特有异常需 SERIALIZABLE(SSI) 防；SQLite 隔离行为又不同。冲突两边都记、点明标准 vs 实现的偏离，不和稀泥。
- 现象↔级别映射据标准 + 实现文档双核；不确定标「待核」，标核实日期。
- 本机实证可选：两个 psql 会话设不同 `SET TRANSACTION ISOLATION LEVEL` 复现脏读/不可重复读/幻读；做了贴命令+真实输出+基线串。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch17（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L17（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- SQL 标准 ISO/IEC 9075（隔离级别/异常规范定义，规范 vs 实现分账核心）；实现侧 PostgreSQL / SQLite 官方文档。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：5 个小主题、单一事务语义主题，默认 1 份。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-10 并发控制 — 报告prompt
任务：为「L4-03·大主题03-10 并发控制」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；锁相容矩阵、wait-for 图、时间戳协议记号、SQL 独占行/块；教辅口吻；广度优先查全、讲到初学者懂不做专家纵深；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-10」小节取全部内容项（03-10.1~03-10.5，查全）；边界：MVCC 实现内幕（版本链/GC）留 L6-04 §04-9，本报告只到入门直觉，显式点边界：
- 03-10.1 基于锁：共享/排他锁、两阶段锁 2PL、严格/强严格 2PL。
- 03-10.2 死锁：预防（wait-die/wound-wait）、检测（wait-for 图）、超时。
- 03-10.3 多粒度锁与意向锁、谓词/间隙锁防幻读。
- 03-10.4 时间戳排序协议与乐观并发控制（OCC 验证阶段）。
- 03-10.5 单机 MVCC 入门：快照读与可见性直觉（版本链/GC 实现留 L6-04 §04-9）。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch18、CMU 15-445 Sp2026 L18-21。
- 规范 vs 实现分歧显式：死锁处理（PostgreSQL 死锁检测自动 abort 一方 vs SQLite 库级锁基本不产生行级死锁）、间隙锁防幻读（InnoDB gap lock vs PostgreSQL 谓词锁/SSI）、MVCC 快照——两边都记、点明。
- wait-die/wound-wait 语义、OCC 三阶段等据一手核对；不确定标「待核」，标核实日期。
- 本机实证可选：Postgres 观察 `pg_locks`、构造死锁看服务端自动检测与 abort；做了贴命令+真实输出+基线串。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch18（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L18-21（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- 实现侧 PostgreSQL / InnoDB / SQLite 官方文档（死锁/间隙锁/MVCC 规范 vs 实现分账）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：5 个小主题、单一并发控制主题，默认 1 份。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-11 日志与恢复 — 报告prompt
任务：为「L4-03·大主题03-11 日志与恢复」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；日志记录格式、LSN/CLR 记号、ARIES 三趟流程、SQL/PRAGMA 独占行/块；教辅口吻；广度优先查全、讲到初学者懂不做专家纵深；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-11」小节取全部内容项（03-11.1~03-11.5，查全）；边界：引擎日志实现细节可延伸 L6-04，本报告为教材级：
- 03-11.1 故障模型与恢复需求（原子性+持久性的运行时保障）。
- 03-11.2 WAL 与日志记录：redo/undo、steal/force 与日志的关系。
- 03-11.3 检查点：普通检查点与模糊（fuzzy）检查点。
- 03-11.4 ARIES：analysis/redo/undo 三趟、LSN、CLR、脏页表/事务表。
- 03-11.5 崩溃恢复流程与幂等重放。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch19、CMU 15-445 Sp2026 L22-23；ARIES 细节可回溯 Mohan 1992 ARIES 论文佐证。
- 规范 vs 实现分歧显式：WAL 实现（SQLite `journal_mode=WAL` vs 传统 rollback journal DELETE 模式；PostgreSQL WAL/`pg_waldump`）——两边都记、点明。
- steal/force 与 redo/undo 需求的对应、ARIES 三趟顺序据一手核对；不确定标「待核」，标核实日期。
- 本机实证可选：SQLite 对比 `journal_mode=DELETE` vs `WAL`；Postgres `pg_waldump` 看 WAL 记录；做了贴命令+真实输出+基线串。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch19（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L22-23（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- 次级：ARIES（Mohan et al. 1992）论文（交叉核对 ARIES 细节）；实现侧 SQLite / PostgreSQL WAL 文档。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：5 个小主题、单一恢复主题，默认 1 份。判定写入 ledger。
```

```text
[P0] L4-03·大主题03-12 （边界）分布式/并行数据库概览 — 报告prompt
任务：为「L4-03·大主题03-12 （边界）分布式/并行数据库概览」产出一份 report-format v3（广度优先·教辅式）报告，落盘到 findings/015-database-systems.md（本为边界过渡章，倾向合并为一节收尾；拆分则 015-database-systems-<slug>-a/-b）。

【v3 格式硬性】`#`→`##`→`###`；`###` 下不写标签、直接正文分段；每项核心说明+充分辅助说明（初学者向，必需）；架构图/分区示意/2PC 流程记号独占行/块；教辅口吻；广度优先但仅入门概念、点到为止（本课边界课，不做纵深）；抬头带基线串+先修/锚点/核实日期。

【小主题清单（=`##` 章节）与内容项】
下游读 round3b 中 L4-03「03-12」小节取全部内容项（03-12.1~03-12.4，查全）；边界课：仅入门概念，深入实现留 L6-04（2PC/Percolator/Calvin/Spanner）、架构选型留 L6-09（复制/分区），报告须显式点出所有下沉边界：
- 03-12.1 数据库系统架构：集中式/C-S/并行/分布式取向对比。
- 03-12.2 并行存储：数据分区（范围/散列/轮转）入门。
- 03-12.3 并行查询处理入门：分区并行、流水线并行的概念。
- 03-12.4 分布式事务入门：2PC 点到为止（协议实现留 L6-04 §04-10）。

【多来源比对强制】
- 每项 ≥2 独立源，优先一手：DBSC 7e ch20-23、CMU 15-445 Sp2026 L24-25。
- 规范 vs 实现：本章为概念概览、无单一实现；如举实例（并行/分布式引擎），标为示例并注明来源层级、不作定论。
- 2PC 两阶段流程/阻塞问题据一手核对；不确定标「待核」，标核实日期。
- 本机实证一般不适用（分布式），如做单机并行示例可选并注明。
- 每章末集中列「来源与时效」。

【权威锚点清单】
- DBSC 7e ch20-23（https://www.db-book.com/）。
- CMU 15-445 Sp2026 L24-25（https://15445.courses.cs.cmu.edu/spring2026/schedule.html）。
- 下沉指向：深入实现见 L6-04 §04-10、架构选型见 L6-09 §09-5/09-6（仅作交叉指引，不在本报告展开）。
- 核实基线日期 2026-07-25。

【粒度判定（下游先做）】先判「1 份 or 拆 N 份 + 理由」：4 个小主题、边界过渡、点到为止，强烈倾向 1 份（可作为课程收尾节）。判定写入 ledger。
```

共 12 条 prompt
